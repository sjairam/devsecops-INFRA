apiVersion: v1
kind: Namespace
metadata:
  name: hgl           #NS name needs to be unique, for LTS it is JUST the stack name made lowercase (ex: curosity or nrsadm)
  labels:
    name: hgl
    istio-injection: enabled      #Enable ISTIO injection for newly created                                                                                                         │
