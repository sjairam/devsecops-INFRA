apiVersion: v1
kind: Namespace
metadata:
  name: acorn           #NS name needs to be unique, for LTS it is JUST the stack name made lowercase (ex: curosity or nrsadm)
  labels:
    name: acorn
    istio-injection: enabled      #Enable ISTIO injection for newly created                                                                                                         │
